import * as BABYLON from "@babylonjs/core";

const applyRotationQuaternionFilter = (
  rotationQuaternion: BABYLON.Quaternion
) => {
  return rotationQuaternion;
};

export default applyRotationQuaternionFilter;
